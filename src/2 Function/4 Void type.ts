function printMessage (message:string) {
    console.log(`This is my message - ${message}`)
}
printMessage("whats Up")

// this function is not returning anything so if u hover over the function name you will find out that the function is actually written as :-
//function printMessage (message:string) : void {
//     console.log(`This is my message - ${message}`)
// }
// printMessage("whats Up")
// here void means that function will not return anything