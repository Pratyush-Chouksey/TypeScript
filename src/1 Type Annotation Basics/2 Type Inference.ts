// hover on the bellow three variable and u will find their type but this will not happen in js
let tech = "TypeScript";
let favNumber = 8;
let tsHard = true;

console.log(typeof tech);  // o/p :- string 
console.log(typeof favNumber);  // o/p :- number
console.log(typeof true);   // o/p :- boolean

// Beacause we are working in a ts file 

/* favNumber = "clkfshv" */ 
// this assigning a string value to number type variable is not possible and hence will throw an error
